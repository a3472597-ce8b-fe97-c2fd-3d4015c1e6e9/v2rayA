name: build

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: bash build.sh
      - uses: actions/upload-artifact@v7
        with:
          path: v2raya
